global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: shape-tree-api
    metrics_path: /metrics
    static_configs:
      - targets: ["api:42068"]
        labels:
          service: shape-tree-api

  # shape-tree-web: no metrics endpoint in the new WASM demo server yet.
