HOST=0.0.0.0
PORT=42069

ADMIN_HOST=0.0.0.0
ADMIN_PORT=42070

# Runtime wasm/css tree. Use an absolute path or path relative to apps/shape-tree-web.
# Demo output: ../../examples/shape-tree-site/content
CONTENT_PATH=../../examples/shape-tree-site/content

INDEX_PATH=Home
SITE_TITLE=

# Public URL used in auth links.
SITE_URL=http://localhost:42069
AUTH_PRIVATE_DIRECTORIES=Private

# Optional Postgres configuration. If all PG* values are set, email login is enabled.
# When running via docker-compose, these are overridden by environment variables.
# PGHOST=postgres
# PGPORT=5432
# PGUSER=shape_tree
# PGPASSWORD=shape_tree
# PGDATABASE=shape_tree
# PGSSLMODE=disable

# Optional SMTP configuration. Required to send login links.
# SMTP_HOST=smtp.mail.me.com
# SMTP_PORT=587
# SMTP_USERNAME=
# SMTP_PASSWORD=
# SMTP_FROM=
# SMTP_TLS=startTLS

OTEL_SERVICE_NAME=shape-tree-web
OTEL_EXPORTER_OTLP_BASE_ENDPOINT=http://jaeger:4318
OTEL_SDK_DISABLED=false
